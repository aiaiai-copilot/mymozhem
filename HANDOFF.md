# HANDOFF

**Date:** 2026-09-24 (двадцатая сессия: **батч 4 UI-среза (Tasks 10–12) исполнен subagent-driven**. SessionStore + useRoomBinding (T10: JWT-декод display-only, join() ровно один раз на effect + стартовый отказ → явный 'disconnected' — рекомендации финревью батча 3 унесены в диспатч и запинаны); /play + shared-компоненты (T11: ветки лобби/вопрос/reveal/финал по дизайну §4, авто-вход §5, anonClient — ruling); /screen (T12: spectator-join 'Экран', read-only, БЕЗ saveGuest — ruling). Финревью батча (fable): **With fixes** — Critical: reveal-вердикт «верно/неверно» был мёртвым кодом (answer.submitted module-private → quiz.answers всегда пуст на клиенте; виден только кросс-пакетной трассировкой visibility — класс батча 3); Important: reconnect после протухания access-токена никогда не восстанавливался (refresh триггерился только REST 401). Fix-волна `2a33aae` (клиентский фолд публичного answer.accepted → вердикт из reveal.awarded; refresh на connect_error ровно один на outage) — re-review поймал новый Critical от самой волны (useMemo после early return → rules-of-hooks крах на каждом входе в комнату) → точечный фикс `d71022c` → micro re-review чистое. Ритм — **батчи по 3 задачи, каждый батч новой сессией**: завершены 4/6. Сессия 19 (батч 3) — в коммитах, см. `git show 02aac53:HANDOFF.md`.)
**Branch:** `main` — **впереди `origin/main` на 8 коммитов** (+ handoff этой сессии будет 9-м; `origin/main` = `30545fb`; push — решение владельца); рабочее дерево чистое. Работа UI-среза идёт на ветке **`ui-slice`** (worktree `.worktrees/ui-slice`) — @ `d71022c` (5 коммитов батча 4 поверх `e5caa45`; всего 14 поверх `a5bb8de`); **мердж батчей в main — решение владельца**. Конвейер на `ui-slice` зелёный на каждом коммите батча (последний полный прогон fix-волны: web 49/49, lint, typecheck, build, boundary-check).
**Worktree'ы:** `.worktrees/rewards-lottery` (ветка `rewards-lottery` @ 33e89ff — срез ф.3 завершён, СОХРАНЁН, снятие — отложенное решение владельца) и `.worktrees/ui-slice` (ветка `ui-slice` — **активный срез**). Леджеры SDD — `.superpowers/sdd/*/progress.md` в основном чекауте (не в git; `git clean -fdx` уничтожит); леджер UI-среза — `.superpowers/sdd/2026-09-23-ui-slice-implementation-plan/progress.md` (батчи 1–4 завершены, разметка батчей, триаж, рекомендации батчу 5 — журнал rulings среза, проектная конвенция).

**Состояние фаз: фаза 1 ЗАКРЫТА (2026-09-09), фаза 2 (Quiz) ЗАКРЫТА (2026-09-10), фаза 3 (Rewards + Lottery + TTL-свип + квиз-начисления) ЗАКРЫТА сверкой (2026-09-23)** — план исполнен 13/13 задач батчами A–E, все критерии §7 дизайна (= §5 пакета) подтверждены артефактами; сверка — `docs/sessions/2026-09-23-phase-3-exit-audit.md`. **UI-срез — В ИСПОЛНЕНИИ: батчи 1–4/6 (Tasks 1–12) завершены на ветке `ui-slice`** (subagent-driven; ритм «3 задачи = 1 сессия» — решение владельца 2026-09-23). Дизайн и план среза: `docs/sessions/2026-09-23-ui-slice-design.md` (§0 — 10 решений владельца) + `docs/sessions/2026-09-23-ui-slice-implementation-plan.md` (17 задач, интерпретации I-1…I-5). Ядро ф.1 (identity, room lifecycle, membership/guest-join/exclusion, realtime полный duplex, event-commit, appSettings, OAuth Google + POST /rooms) и Quiz-срез ф.2 (SDK рантайм-контракт app-модулей, app-runtime «командный хост» в ядре, пакет `@mymozhem/app-quiz` — манифест, 7 типов событий, чистый редьюсер, скоростная шкала, анти-бот REQ-RT-013, роль SPECTATOR) — реализованы и слиты в `main`. Сверки: `2026-09-09-phase-1-exit-audit.md`, `2026-09-10-phase-2-exit-audit.md`, `2026-09-23-phase-3-exit-audit.md`. Этап продукта — MVP. Метод — AIDD / Specification-Driven.

## Как войти в контекст за одно чтение

1. `CLAUDE.md` — рамка проекта и интеграция с superpowers (правило «решено vs открыто»).
2. Этот файл целиком.
3. `docs/spec/normative-package-v1.2.md` — источник истины: 11 ADR, ~90 требований, §5 фазовый план.
4. `docs/spec/amendment-v1.3-phase-remapping.md` — утверждённая пере-разметка фаз.
5. `docs/spec/amendment-v1.4-admin-contour.md` — перенос REQ-ID-012 (админ-контур) в ф.4.
6. `docs/sessions/2026-09-09-phase-1-exit-audit.md`, `docs/sessions/2026-09-10-phase-2-exit-audit.md` и `docs/sessions/2026-09-23-phase-3-exit-audit.md` — сверки критериев выхода ф.1, ф.2 и ф.3 (все три фазы закрыты).
7. `docs/sessions/2026-09-09-quiz-module-design.md` — утверждённый дизайн Quiz-среза (§0 — решения владельца; §3 — erratum нейминга dotted-lowercase) — читать как образец перед дизайном следующего app-модуля.
8. `docs/sessions/2026-09-10-rewards-lottery-design.md` — **утверждённый дизайн фазы 3** (§0 — решения владельца, включая гейт 2: реализация после первого живого события).
9. `docs/sessions/2026-09-10-rewards-lottery-implementation-plan.md` — **план фазы 3** (13 задач, батчи A–E по 3 задачи; интерпретации плана — его раздел Self-review, зафиксированы для ревью владельцем). Вход для сессии исполнения.
10. **`docs/sessions/2026-09-23-ui-slice-design.md` + `docs/sessions/2026-09-23-ui-slice-implementation-plan.md` — утверждённые дизайн и план следующего (UI) среза** — вход для сессии исполнения (subagent-driven).
10. `.superpowers/sdd/*/progress.md` — леджеры исполнения срезов (не в git, только на этой машине; `git clean -fdx` уничтожит). Леджеров срезов исключения и OAuth на машине нет — их история: планы + `git log` (исключение `89d887f..c2e60da`, OAuth `44db424..ab973f4`).
11. `docs/roadmap.md` — траектория прототип→MVP→платформа→BaaS.

Исполненные планы и дизайны прежних срезов читать при разборе истории — их работа в коммитах. Состояние на конец прошлой сессии: `git show 02aac53:HANDOFF.md`.

## Следующее действие

**Следующее действие — БАТЧ 5 UI-СРЕЗА (Tasks 13–15), новой сессией, subagent-driven, в существующем worktree `.worktrees/ui-slice` (ветка `ui-slice`).** Ритм — решение владельца 2026-09-23: **батчи по 3 задачи, каждый батч новой сессией**; после батча — финальное ревью диапазона батча, HANDOFF, стоп. Батчи: ~~4 = Tasks 10–12~~ (завершён), **5 = 13–15** (лендинг/OAuth-гейт/setup; консоль квиз; консоль участники/призы/розыгрыш), 6 = 16–17 (Playwright smoke + CI; чеклист + exit-аудит). Вход: план `docs/sessions/2026-09-23-ui-slice-implementation-plan.md` (Global Constraints и Review Focus обязательны; интерпретации I-1…I-5) + дизайн `docs/sessions/2026-09-23-ui-slice-design.md` (§0 — 10 решений); леджер `.superpowers/sdd/2026-09-23-ui-slice-implementation-plan/progress.md` (батчи 1–4 завершены, briefs всех задач уже извлечены туда же — `task-N-brief.md`). **Рекомендации финревью батча 4 к батчу 5:** (1) host-консоль — третий потребитель join/roster/reveal-клея play/screen → вынос общего хука (deferred-minor T12 активируется); (2) если консоль рендерит in-app `<Link>` на /play|/screen — key'ить страницы по `:code` или обрабатывать смену параметра (это активирует отложенные one-shot-join миноры T11/T12); (3) Standings — поправить комментарий «доверяем местам из payload» при касании (код рендерит i+1 — бриф разрешает). **Рекомендации батчу 6:** Task 16 Playwright обязан включить сценарий «answer → reveal показывает Верно/Неверно» (Critical батча 4 — тот же класс «ловится только живым прогоном», что content-type батча 3 — не ослаблять смоук). **Урок батча 4 для диспатчей:** не строить клиентскую логику на module-private событиях (answer.submitted клиенту структурно недоступно) — диспатчи страниц батча 5 должны сверять visibility каждого потребляемого события с манифестом. Ключевые сути среза прежние: одна комната = одно приложение (пин ф.3); CORS не включается (same-origin static); correctAnswers клиенту структурно недоступны; Playwright только против локального compose-стенда. Стек клиента запинан ruling'ом батча 2: **react 18.3.1 / react-router-dom 6.30.6**. Мердж батчей в main и push — решения владельца.

**Решения владельца по открытым вопросам ф.3 (сессия 16, все 5 закрыты):**
- **C-9.1 (REQ-RWD-014) → `io: 'input'` в SDK:** toRegisteredSchema генерирует JSON Schema в input-режиме — defaulted ключи необязательны в артефакте, configure `{}` проходит гейт, дефолты применяет zod-parse в handler'е. Гейт ядра остаётся verdict-only. SDK 1.7.0. **Исполнено батчем 1 UI-среза (`71d72c0`, ветка ui-slice).**
- **Свипнутый гость в drawPool → добавить фильтр:** `identity: { deletedAt: null }` в `listActiveParticipantPool` — TTL-истёкшие гости вне пула розыгрыша. **Исполнено батчем 1 UI-среза (`a55f764`, ветка ui-slice).**
- **D-12.2 (revoke vs winner-exclusion) → fail-safe подтверждён продуктово:** отозванный победитель навсегда вне пула этого приза (двойной выигрыш одного лица невозможен). Код не меняется; поведение запинано e2e (re-draw после revoke уходит другому).
- **Окно гонки свипа (C-8.1) → перенесено в ф.4:** закрытие (tx-aware гард / serializable+retry) — design-level задача разреза фазы 4. Принятый риск MVP задокументирован в `guest-sweep.service.ts:22-29`.
- **Шов registered-join (D-12.1) → разобрать в UI-срезе:** при дизайне UI решить, как registered играют в MVP (нужен ли HTTP registered-join — JoinController сейчас игнорирует Authorization и всегда создаёт GUEST), и если нужен — включить в план среза. **Исполнено в дизайне UI-среза (решение №2, §0): участники — только гости; JoinController не меняется; шов — задел до продуктового требования.**

**Фаза 4 разрезана решением владельца (сессия 15):** до первого события — SEC-008 (security-заголовки), минимум OPS-004 (метрики), опционально REQ-RT-008 (архивация логов), + закрытие окна гонки свипа (C-8.1, решение сессии 16); OPS-006 (бэкап-регламент) — после юриста/реальных PII; нагрузочный тест и лимиты режимов (RT-014/015, ID-019, RT-011б, ID-012, ID-018) — после первого живого события (оно измеряет нагрузочный профиль — продуктовую ставку). Обоснование: фильтр амендмента v1.3 — кратчайший путь к первому живому событию; харднинг от акторов, отсутствующих на MVP-масштабе, — задел.

**Manual smoke OAuth с реальными Google-кредами** перед первым живым событием — действие владельца. Гейты над фазами — см. раздел ниже (юрист открыт).

**Остаточные риски, принятые мерджем (realtime-срез):**
- **Duplicate-acceptance в subscribe** (осознанный trade-off дизайна §4): join каналов — ДО чтения лога, поэтому событие, закоммиченное между join и чтением, придёт и live, и в snapshot. Клиент без seq/cursor (REQ-RT-011a) дедуплицировать не может — принято для MVP; ссылка для фазовой работы над курсором (ф.4).
- **Deferred-миноры финального ревью (ride, триаж «не гейтят»):** M-2 — мёртвая инжекция `config` в RealtimeGateway (убрать при следующем касании); M-4 — cross-socket timing в live-visibility e2e (микроскопическое окно флейка; маркер на organizer-сокете сделает детерминированным); M-5 — `asLogEvent` fallback fail-open (`?? row.visibility`) — fail-closed throw при следующем расширении enum EventVisibility. (M-3 — stale registry entry — **закрыт срезом исключения**, `74035f9`.)
- Риски прошлых срезов (refresh-ротация strict, access-токены ≤15 мин после терминации, `/health/ready` 503) неизменны — `git show 3d7de83:HANDOFF.md`.

**Санкционированные отклонения Quiz-среза** (все прошли ревью, полный журнал — леджер `.superpowers/sdd/2026-09-09-quiz-implementation-plan/progress.md` и §4 exit-аудита ф.2):
1. **Ruling pre-flight:** плотность seq ассертится относительно baseline (lifecycle-события уже в логе), не абсолютных 1..20.
2. **T5:** `describe('AppRegistryModule')` переведён на `register([])` — статический импорт стал fail-closed.
3. **T7:** `AppRuntimeModule.register` получил `imports: [PrismaModule, MembershipModule, RealtimeModule]` — без них Nest DI не разрешает deps AppRuntimeService (int-spec строил сервис вручную, минуя контейнер).
4. **T8:** гард лимита участников обёрнут `if (role !== 'spectator')` — count-запрос (PARTICIPANT) не тронут, прочие гейты (exclusion по IP, rate-limit) сохранены.
5. **T9 (ruling, erratum):** wire-имена событий квиза — dotted-lowercase (`question.opened`, `answer.submitted`, `question.closed`, `game.finish`, `answer.accepted`, `question.revealed`, `game.finished`) под существующую `shortEventNameSchema`; ослабление схемы отклонено (blast radius на все приложения, REQ-CTR-008). TS-идентификаторы `*Payload` остались camelCase.
6. **T10:** `reduceQuiz` игнорирует `payload.totals`, перестраивает totals из `awarded` — проекция не доверяет производным данным payload (запинено тестом с заведомо неверным totals).
7. **T11 (fix round 1):** standings — плотный ранг (1,2,2,3), не competition ranking; различающий тест {100,50,50,30}.
8. **T13:** e2e без override'ов `APP_MANIFESTS`/`APP_RUNTIME_MODULES` — под тестом фактический composition root (сильнее буквы плана); локальная копия `readRoomLog` в спеке (barrel ядра его не экспортирует — утверждение плана устарело); противоречие плана о standings/total 0 разрешено в пользу сценария 1 (totals только из awarded — гость, не отвечавший ни разу, в standings не попадает).

**Опыт Quiz-среза для следующих:**
- **jest testPathPattern матчит ПУТЬ, включая имя worktree** — `.worktrees/quiz/` в пути делает `-- quiz` матчем ВСЕХ e2e-сьютов (каждый прогон = полная лана ~10 с/сьюта). Для точечного прогона использовать уникальное имя файла (`-- quiz.e2e`).
- **Live-кадр может прийти раньше ack publish** (fan-out синхронен с коммитом) — waiters навешивать ДО триггерящего publish, иначе тест повиснет на flush (прецедент: сценарий 1 quiz.e2e, таймаут 180 с).
- **Сырой `$queryRaw` лога отдаёт visibility lowercase** (`'public'`), не Prisma-имя — фильтры по логу в e2e учитывают (та же ловушка, что asLogEvent в realtime-срезе).
- **e2e на проводе снова поймал то, что unit не видел** — порядок «waiter до publish»; DI-разрешение модулей (`imports` в register) видно только через реальный контейнер.
- **Финальное ревью подтвердило:** REQ-RT-007 (payload-нейтральность) на app-пути покрыт `app-runtime.int-spec.ts` тестом 8 (20 конкурентных dispatch, чередование 0/4KB, seq плотные baseline..+39).

**Швы Quiz-среза для будущих планов:**
- **Рантайм-контракт app-модуля (SDK 1.5.0):** `AppRuntimeModule` (manifest + reduce + handlePublish), `AppCommit`, `AppHostContext` (actorId, now ISO, roomId), `AppRejection` — единственный путь механики к логу; отказ модуля — типизированный код из `CONTRACT_ERROR_CODES`, до коммита.
- **Диспетчер `AppRuntimeService`:** гейты до вызова модуля (room ACTIVE → пин `(appId, manifestVersion)` → membership → SPECTATOR → clientInitiated → owner-схема payload), коммит пакетом событий одной транзакцией через `outbox.run`; per-room сериализация `RoomSerializer` (FIFO, identity-checked cleanup); проекция — `AppProjectionCache` + replay из лога, `invalidateProjection(roomId)` для пересоздания.
- **Связывание модулей — только в composition root** (`apps/server/src/app.module.ts`): `AppRegistryModule.register([...])`, `AppRuntimeModule.register([...])`, оба global (шов Task 5/7).
- **Имена событий app-модулей — dotted-lowercase** (`shortEventNameSchema`), полный wire-тип `<appId>.<shortName>`.
- **SPECTATOR (REQ-ID-011):** wire lowercase `spectator` ↔ Prisma `SPECTATOR` в одной точке membership; зритель вне лимита участников; publish зрителю → `PUBLISH_FORBIDDEN` на гейте диспетчера.
- **`correctAnswers` module-private без аннотации** — fail-safe по умолчанию (REQ-CORE-008); чит-тест ф.2 подтвердил: недоступно ни по одному каналу, включая организатора и replay.

**Швы батча B (rewards-контур ядра) для будущих планов:**
- **`RewardsService` (core/rewards):** `executeEffects(tx, roomId, sourceAppId, effects)` — вызывается ТОЛЬКО из чужого `outbox.run` (вложенный run запрещён; связка с диспетчером — Task 7 через токен `AWARD_EFFECT_HANDLER`). `createPrize/listAwards/fulfill/revoke` — REST-контур организатора, свои `outbox.run`; автомат AWARDED→FULFILLED|REVOKED идемпотентен (no-op повтор, cross-переход → REWARD_ALREADY_RESOLVED); revoke возвращает quantity ровно один раз и только при состоявшемся переходе. `assertOrganizer` одноклиентный (findActiveMembership без tx — прецедент exclude, TOCTOU принят).
- **Награждение призом:** `INSERT ... ON CONFLICT (partial-index) DO NOTHING` (дубль → no-op БЕЗ списания фонда) → re-read по ключу индекса → ограниченный декремент (`$executeRaw`, 0 строк → PrizeFundExhaustedError откатывает insert) → `commitRewardsEvent` — всё в одной tx (REQ-RWD-003/010).
- **`EventLogService.commitRewardsEvent(tx, roomId, name, payload, actorId=null)`** — статический путь `commitStaticEvent` (общий с core.*, рефактор батча B); реестр `REWARDS_EVENTS` core-owned, клиентский publish закрыт (namespace 'rewards' не appId → EVENT_UNKNOWN_TYPE).
- **REST rewards:** `POST /rooms/:id/prizes` (201), `GET /rooms/:id/rewards`, `POST /rooms/:id/awards/:awardId/fulfill|revoke` (200). **ВНИМАНИЕ: RewardsModule НЕ подключён в composition root** (`apps/server/src/app.module.ts`) — эндпоинты существуют только в testing-модулях; подключение + handler-провайдер + @Global — Task 11, e2e-приёмка — Task 12.
- **HTTP-фильтр:** guarded-ветки `ContractError` и `RealtimeError` по членству кода в `STATUS_BY_WIRE_CODE` (реалтайм-коды в мапе: ROOM_NOT_ACTIVE 409, ACTOR_NOT_MEMBER 403 — обе ветки запинены тестами).
- **Модели Prisma rewards:** `prisma.prize/.award/.pointsGrant`, enum AwardStatus uppercase БЕЗ @map (SQL индекса зависит); Award.prizeId nullable (REQ-RWD-002b, NULL не конфликтует в индексе). Конфиг `CLEANUP_INTERVAL` (сек, 300…86400, дефолт 3600) — потребитель Task 8.

**Швы батча C (app-runtime эффекты + свип + лотерея) для будущих планов:**
- **`AWARD_EFFECT_HANDLER` (core/app-runtime/effects.ts):** токен + интерфейс `executeEffects(tx, roomId, sourceAppId, effects)` — единственный шов «ядро не зависит от rewards» (REQ-RWD-001). В AppRuntimeService — `@Optional()` 8-й параметр конструктора; эффекты без capability 'rewards' манифеста или без подключённого исполнителя → `CAPABILITY_UNAVAILABLE` до исполнения; исполнение — в `outbox.run` ДО цикла коммитов (отказ эффекта откатывает события). Гварда `commits.length > 0 || effectHandler` сохраняет fast-path пустого publish (effect-only publish транзакцию открывает, foldCommitted — только при непустом committed).
- **`MembershipService.listActiveParticipantPool(roomId)`** — снапшот пула розыгрыша: активные PARTICIPANT, без ORGANIZER/SPECTATOR/исключённых/soft-deleted, комната не удалена. **НЕ фильтрует `identity.deletedAt`** (свипнутые гости в пуле — открытый вопрос владельцу, см. выше). Вызывается на каждый dispatch capability-модуля (снапшот на момент команды).
- **TTL-свип (core/identity):** `GuestSweepService.sweepExpiredGuests(now?)` — анонимизация (displayName/email → NULL, deletedAt, id сохранён) + отзыв живых сессий одной tx; база TTL — createdAt; приостановка — через `ANONYMIZATION_GUARDS` (токен в identity, реализация `RewardsAnonymizationGuard` в rewards, AWARDED = открытая). **Связка гардов требует @Global wiring-модуля** (прецедент TestAnonymizationWiringModule). `IdentitySweepModule` (scheduler, @nestjs/schedule ^6.1.3, CLEANUP_INTERVAL) — **НЕ подключён нигде** до Task 11; тесты вызывают sweepExpiredGuests напрямую. IdentityModule теперь импортирует ConfigModule.
- **`@mymozhem/app-lottery`:** `createLotteryApp()` → `{ manifest, runtime }`; LOTTERY_APP_ID 'lottery', MANIFEST_VERSION 1, capabilities ['rewards']; команда `draw.run` (только ORGANIZER, payload drawId+prizeId uuid) → echo + `draw.completed` (actor server) + эффект award.prize; повтор drawId — re-emit прежнего результата БЕЗ эффекта (самолечение клиента, дедуп редьюсера по drawId); verified-режим отсекает GUEST из пула; прежние победители этого приза вне пула (UX, индекс — страховка). Инфраструктура — дословное зеркало app-quiz. **Не зарегистрирован в composition root** — Task 11, e2e-приёмка — Task 12.

**Швы батча D (quiz@2 + composition root + e2e лотереи) для будущих планов:**
- **Quiz@2 (SDK-контракт):** `QUIZ_MANIFEST_VERSION = 2`, `capabilities: ['rewards']`; `handleQuizPublish` возвращает `AppPublishResult` (`{commits, effects}`); closeQuestion мапит awarded (points > 0) в `{ kind: 'award.points', identityId, points, reason: 'quiz.round' }` — порядок эффектов = порядок awarded (seq-ранжирование). Идемпотентность повторного question.closed — существующий гейт ROUND_NOT_OPEN до эффектов. Табло квиза и ledger — осознанное дублирование с инвариантом «менять вместе» (сходимость — e2e Task 13).
- **Composition root фазы 3 (apps/server/src/app.module.ts):** `ScheduleModule.forRoot()` + `AppRegistryModule.register([quiz.manifest, lottery.manifest])` + `AppRuntimeModule.register([quiz.runtime, lottery.runtime])` + RewardsModule (@Global, `{provide: AWARD_EFFECT_HANDLER, useExisting: RewardsService}`, RewardsService `implements AwardEffectHandler`) + IdentitySweepModule + AnonymizationGuardsModule (@Global, фабрика `[RewardsAnonymizationGuard]` → ANONYMIZATION_GUARDS). Rewards-эндпоинты и свип теперь живут в реальном AppModule (батч B/C подключались только в testing-модулях).
- **Boundary `rewards-only-through-di-tokens` (.dependency-cruiser.cjs):** from `^packages/core/src` минус `^packages/core/src/rewards`, `index.ts` и `'[.]int-spec[.]ts$'` (ruling C-F.1: спеки — миниатюры composition root); to `^packages/core/src/rewards`. Probe в verify-guardrails пишет `__probe-rewards-boundary.ts` с ОТНОСИТЕЛЬНЫМ импортом `'../rewards/rewards.module'` (subpath `@mymozhem/core/src/...` depcruise не резолвит — ловился not-to-unresolvable, а не правило).
- **Lint-запрет Math.random (eslint.config.js):** `no-restricted-syntax` на `CallExpression[callee.object.name='Math'][callee.property.name='random']` в `packages/app-*/src/**/*.ts` (REQ-RWD-011); probe в guardrails.
- **IdentitySweepModule в реальном AppModule:** `onModuleDestroy` делает идемпотентный `doesExist`-гард на deleteInterval — e2e `app.init()` без listen не запускает onApplicationBootstrap, интервала нет (асимметрия хуков Nest).
- **lottery.e2e-spec.ts:** 7 сценариев на реальном AppModule без override'ов; registered-участник сидится на уровне БД (HTTP registered-join отсутствует — JoinController всегда GUEST, ruling D-12.1); сценарий verified — один детерминированный розыгрыш (winner-exclusion делает «10 розыгрышей одному» невозможным, ruling D-pre.2); re-draw после revoke уходит ДРУГОМУ участнику (редьюсер не забывает draw.completed, ruling D-12.2).

**Швы батча E (quiz-points e2e + exit-аудит ф.3) для будущих планов:**
- **`quiz-points.e2e-spec.ts` (приёмка design §7.8):** тест 1 — 3 гранта pointsGrant (reason quiz.round, sourceAppId quiz, REQ-RWD-002b), суммы ledger ≡ суммам awarded из reveal в сыром логе (P1=1900, P2=1000), повторный question.closed → ROUND_NOT_OPEN без новых грантов, PII-цикл по всему логу ('Гость'). Тест 2 (ruling E-13.1) — configure с пином quiz@1 → `APP_MANIFEST_UNKNOWN`: **пин на версию вне boot-time реестра недостижим сервисным путём** (configure — app-registry.service.ts:44, activate перепроверяет — room.service.ts:183, REQ-CORE-007); состояние «ACTIVE на отсутствующей версии» возникает только после редеплоя. Структурный ассерт по `code` (AppManifestUnknownError не в barrel core). TRUNCATE e2e явно включает rewards."Award"/"Prize"/"PointsGrant" (E-pre.1).
- **Скетчи плана, предполагающие configure/activate произвольной версии манифеста, невоспроизводимы в одном прогоне** — учитывать при планировании: boot-time реестр + verdict-only гейт делают такие сценарии достижимыми только int-уровнем (app-runtime.int-spec кейс 4 — MODULE_UNAVAILABLE) или raw-UPDATE.
- **Exit-аудит ф.3** — `docs/sessions/2026-09-23-phase-3-exit-audit.md`: 8 критериев §7 с артефактами файл:строка, §6 — 5 открытых вопросов владельцу со статусами.

**Швы батча 1 UI-среза (SDK 1.7.0 + drawPool + wire-схемы) для будущих планов:**
- **SDK 1.7.0 (C-9.1, REQ-RWD-014):** `toRegisteredSchema` генерирует JSON Schema в `io: 'input'` — defaulted ключи необязательны в артефакте. Конвертер ОБЩИЙ для appSettings и event-payload: io:'input' действует и на схемы событий (live-изменения нет — `.default` в event-схемах quiz/lottery не используется; первый defaulted ключ в событии изменит поведение гейта — опциональный пин-тест отложен). Дефолты применяет zod-parse handler'а модуля; гейт ядра verdict-only (ajv без useDefaults/coerceTypes — `default`-аннотации в артефакте инертны на гейте).
- **`isUnrepresentablePipe` (define-app.ts):** fail-closed отказ `SCHEMA_NOT_REPRESENTABLE` на `.transform()/.pipe()` — zod 4 в input-режиме перестал бросать сам (молча эмитит input-сторону, дропая post-transform валидацию); детектор восстанавливает прежний author-facing контракт (ADR-008 silent-loss class). Опирается на `_zod.def.type` — tripwire при апгрейде zod: существующая `.transform()`-фикстура. Чуть строже старого zod для transform-free validation pipes (`z.string().pipe(z.string().min(2))`) — fail-closed, осознанно.
- **drawPool (P2):** `listActiveParticipantPool` += `identity: { deletedAt: null }` — swept-гость (TTL-анонимизация) вне пула розыгрыша; тест в `membership.service.int-spec.ts` (describe listActiveParticipantPool).
- **Новые wire-схемы SDK (потребители — Task 5 и web-клиент):** `configureRoomRequestSchema` (strictObject; `settings: z.record(z.string(), z.unknown())` — семантика на app-registry REQ-CORE-007, здесь форма), `rosterEntrySchema`/`rosterResponseSchema`/`rosterRoleSchema` (wire-роли lowercase enum incl. moderator; `displayName` nullable — swept-гость), `listPrizesResponseSchema` (обёртка над `prizeResponseSchema`).

**Швы батча 2 UI-среза (membership-asserts + HTTP-транспорт + apps/web scaffold) для будущих планов:**
- **`MembershipService` (core/membership):** `assertMember(roomId, identityId)` → `ActorNotMemberError`; `assertOrganizer` → `ActorNotOrganizerError` (оба из `./membership.errors`, в фильтре 403); `listRoster(roomId)` — активные membership'ы `orderBy joinedAt asc`, **сознательно НЕ фильтрует `identity.deletedAt`** (swept-гость остаётся с `displayName: null` — решение №10; в отличие от `listActiveParticipantPool`).
- **HTTP-контур (transport):** `RoomsLifecycleController` — `POST /rooms/:roomId/configure|activate|complete|cancel` (organizer-only, `@HttpCode(200)`, ответ `createRoomResponseSchema.parse`); `MembersController` — `GET /rooms/:roomId/members` (любой член, роли wire-lowercase); `RewardsController` += `GET /rooms/:roomId/prizes` (organizer, `RewardsService.listPrizes` — зеркало listAwards). Gate-order везде: `authenticate` → `z.uuid().parse(roomId)` → доменный гейт роли → парсинг body (не-организатор получает 403, не 400 — validation oracle закрыт). actorId — только `claims.sub`.
- **Exception-фильтр:** все 5 `ROOM_ERROR_CODES` в `STATUS_BY_WIRE_CODE` (переходы/конфликты → 409, прецедент ROOM_NOT_ACTIVE); неизвестный 6-й код — fail-closed 500 + лог; compile-time tripwire (satisfies-подтаблица) — deferred. Негативный кейс manifestVersion (0/1.5 → 400) запинан на транспортной границе (`it.each` в rooms-lifecycle int-spec).
- **apps/web (`@mymozhem/web`):** Vite 8 + React 18.3.1 + react-router-dom 6.30.6 (пин ruling'ом батча 2 — план «React 18, react-router 6»; апгрейд — решение владельца); dev-proxy `/rooms|/auth|/health|/socket.io`(ws) → :3000; 4 маршрута-заглушки. **Boundary:** `web-only-through-sdk-and-app-packages`, `web-socketio-only-in-realtime` (socket.io-client только в `src/realtime`), `web-no-cross-app-imports` (нет импортов apps/*); depcruise круизит .tsx; `enhancedResolveOptions` (exportsFields/conditionNames) добавлен для exports-only пакетов.
- **Guardrails:** `expectFailure(label, cmd, expectOutput)` — каждый probe ассертит сигнал (имя правила/REQ-маркер) в выводе инструмента, не только exit-code (vacuous-probe класс закрыт для всех 8 probes; fail-closed при переименовании правила).
- **Static-serving:** `apps/server/src/static-web.ts` (`registerStaticWeb`) — `WEB_STATIC_DIR` задан → `@fastify/static` + SPA-fallback в encapsulated scope `{prefix:'/'}` ПОСЛЕ явного `app.init()` (Nest владеет root-404; inline `setNotFoundHandler` невозможен — avvio); fallback только для не-API GET (`API_PREFIXES` = vite-proxy префиксы), API-404 — JSON `{code:'NOT_FOUND'}` (mode-dependent с REQUEST_INVALID — deferred, клиентский бакетинг — рекомендация к Task 7). `WEB_STATIC_DIR` в config.schema (optional) и compose (`/app/apps/web/dist`).
- **pnpm-workspace.yaml:** `peerDependencyRules.allowedVersions { '@types/react': '19' }` — types-only конфликт в prebuilt `@prisma/studio-core`; own types apps/web чисто 18 (проверено re-review). После reinstall может потребоваться ручной `prisma:generate` (postinstall выключен allowBuilds — известное поведение репо).

**Швы батча 3 UI-среза (API-клиент + realtime-клиент + фолд-модель) для будущих планов:**
- **`ApiClient` (apps/web/src/api):** `ApiError { code, status }` прозрачен для неизвестных wire-кодов (ruling батча 2; запинан тестом `NOT_FOUND`/`SOME_FUTURE_CODE` — generic-бакетинг забота слоёв выше). zod-валидация ответов fail-loud; 401 → ровно один `refresh()` + один retry (флаг `allowRetry`); `credentials: 'same-origin'`; 14 endpoint-обёрток над SDK-схемами (пути/схемы сверены с контроллерами, lifecycle-ответы — `createRoomResponseSchema`, exclude шлёт без тела при отсутствии `reason`). **`content-type` выставляется только при `body !== undefined`** — fastify 5 отвечает `400 FST_ERR_CTP_EMPTY_JSON_BODY` на `content-type: application/json` с пустым телом (Critical финревью: fetch-стабы в клиентских тестах и header-less int-спеки сервера оставляли дыру между двумя per-task проверками; поймано whole-batch ревью с эмпирикой — класс «ловится только живым прогоном», аргумент не ослаблять Playwright-смоук Task 16). `TokenProvider` — интерфейс; реализация — SessionStore (Task 10).
- **Realtime-клиент (apps/web/src/realtime):** `socket-like.ts` — единственный импортёр socket.io-client (boundary); токен читается на каждый handshake (`auth: cb`); имена сообщений — только `REALTIME_MESSAGES`. Ack-ошибки → `ApiError(code, 0)` через `contractErrorPayloadSchema` (enum-strict: код вне `CONTRACT_ERROR_CODES` → ZodError — осознанное расхождение с REST-прозрачностью, ruling подтверждён первоисточником: `wireCodeOf` отдаёт только enum). Reconnect после join → re-subscribe → `onResync` (полный перефолд). **Gateway при ошибке subscribe соединение НЕ разрывает** (`registry.remove` + `socket.leave` + ack-ошибка, `realtime.gateway.ts:176-186`) — reconnect-ветка имеет `.catch` → явный `'disconnected'` (иначе тихий мёртвый «live»; retry-стратегия — забота потребителя Task 10+). `join()` НЕ идемпотентен (диспатч Task 10: один раз на effect; стартовый отказ join — обработать там же).
- **Фолд-модель (apps/web/src/state):** `LogStore` — live до snapshot в буфер, `all()` = snapshot+buffered+live, дуп окна handshake запинан тестом (не «исправлен»; лечение — re-subscribe → reset + перефолд). `toAppLogEvent` — локальный seq по порядку приёма, `recordedAt` только отображение (баллы в payload `question.revealed`). Проекции — через `reduceQuiz`/`reduceLottery` с фильтром `${appId}.`; `deriveRoomStatus` из lifecycle-событий (REQ-RT-010, последнее побеждает); `shouldRefetchRoster` — ровно `{'quiz.question.revealed','quiz.game.finished','lottery.draw.completed'}` (Review Focus 2). Golden-фикстура `quiz-log.fixture.ts` — по zod-схемам `quiz-events.ts`, recordedAt фиксированной строкой; ассерт локального seq в `answers` пинит адаптер/фильтр.

**Швы батча 4 UI-среза (SessionStore + страницы участника/проектора) для будущих планов:**
- **`SessionStore` (apps/web/src/state/session.ts):** access-токен только в памяти (TTL ≤ 15 мин), localStorage — только переживающая перезагрузку идентификация (`mm.guest.session` {code, displayName} гостя, `mm.host.roomId` организатора). `implements TokenProvider` + `setAccessToken` (вне интерфейса — потребители страниц). `decodeAccessClaims` — base64url-декод БЕЗ верификации, display-only (Review Focus 1; все решения доступа серверные).
- **`useRoomBinding` (apps/web/src/state/use-room-binding.ts):** `join()` ровно один раз на effect (неидемпотентен); стартовый отказ join → явный `'disconnected'` (gateway соединение НЕ разрывает — без catch UI висел бы в 'connecting'); cleanup обнуляет connRef ПЕРЕД close (publish после teardown → NOT_CONNECTED). Refresh-хук на connect_error: ровно один `tokens.refresh()` на outage-стрик (флаг до вызова, сброс на успешном connect), провал refresh → 'disconnected' без unhandled rejection; connect_error до первого join refresh не жжёт. Свежий токен в handshake — auth-cb socket-like перечитывает провайдер на каждую попытку.
- **Reveal-вердикт участника — из публичных сигналов:** `projectAnsweredActors` (projections.ts) фолдит публичные `quiz.answer.accepted` (payload `{questionIndex, actorId}`), keyed по questionIndex текущего вопроса (stale-accepts прошлого вопроса отфильтрованы). Вердикт: не отвечал → «не успели»; `myId ∈ reveal.awarded` → «Верно!»; отвечал вне awarded → «Неверно.». **`answer.submitted` module-private — клиенту недоступно структурно** (ProjectionService отдаёт участнику только PUBLIC): не строить клиентскую логику на module-private событиях — урок Critical батча 4, сверять visibility каждого потребляемого события с манифестом при диспатчах страниц батча 5.
- **Страницы (паттерны для консоли батча 5):** `anonClient` = module-level ApiClient с null-TokenProvider (join/refresh — auth:false); ConnectionBanner НЕ рендерится при roomId===null (useRoomBinding(null) держит 'connecting' навсегда — deferred-minor T10); roster — listMembers на subscribe + рефетч по shouldRefetchRoster через SessionStore-backed клиент; авто-вход §5: silent refresh → та же identity, TTL протух → новый join + баннер «вы вошли заново»; UX-маппинг wire-кодов (все коды сверены с sdk error-codes финревьюм). Screen: join role 'spectator' displayName 'Экран', БЕЗ saveGuest (spectator не затирает guest-сессию участника под тем же ключом localStorage), publish даже не деструктурируется (§8 read-only).
- **Порядок хуков — машинного гейта нет:** eslint.config.js без eslint-plugin-react-hooks; rules-of-hooks крах (хук после early return) ловится только рантаймом/review. Все хуки страниц — до любого условного return. Кандидат-follow-up владельцу — подключить eslint-plugin-react-hooks.

**Follow-up пакеты, подбираемые будущими планами явно:**
- **Кандидаты владельцу из финревью батча 4 UI-среза:** (1) подключить eslint-plugin-react-hooks — rules-of-hooks крах батча 4 линтом не ловится, только рантайм/review; (2) серверный: spectator-join ставит refresh-куку и затирая куку участника на одном браузере (JoinController ставит REFRESH_COOKIE всем join'ам; кандидат-фикс — не ставить куку spectator'ам; edge case репетиции/один ноутбук).
- **Deferred-миноры батча 4 UI-среза (триаж финревью батча — все 13 stay-deferred):** T10: stale-settle race при смене roomId в полёте join (нет in-app пути смены комнаты); useRoomBinding без юнит-тестов (plan-justified, нет @testing-library/react); roomId===null держит 'connecting' (страницы баннер не рендерят). T11: Standings рендерит i+1 вместо серверного place при дележе (бриф разрешает; поправить комментарий при касании — рекомендация батчу 5); гонка двойного клика по ответу (сужена фиксом Critical-1); JoinForm одноразовый :code и T12 смена :code без перезапуска join (активируются при in-app Link — рекомендация батчу 5); Winners показывает prizeId (названия — контур консоли); displayNameOf без теста. T12: дубль new Date() на рендер; дублирование join/roster/reveal-клея с play-page (вынос общего хука — рекомендация батчу 5); пустой Standings на финале чистой лотереи; O(n) reveal-скан. Из declined-to-judge финревью: 6 строк, все stay (PUBLISH_FORBIDDEN серверный; module-private visibility — принятое решение ф.2; параллельные roster-refresh idempotentны; StrictMode double-mount stateless; two-room flow — дизайн §4). Подробности — леджер UI-среза.
- **Deferred-миноры батча 3 UI-среза (триаж финревью батча — все stay-deferred):** implicit auth:true у 12 обёрток не ассертится в табличном тесте; auth:true без токена + 401 жжёт refresh+retry (недостижимо через endpoints.ts, latent); RED = module-resolution (greenfield-форма); `'live'` выставляется до завершения re-subscribe (транзиент, самолечится onResync); контракт «applySnapshot не идемпотентен без reset()» не задокументирован (путь запинан тестом); appId литералами в projections.ts (brief-псевдокод идентичен); HOST в golden-фикстуре не используется (потребители Task 10+); косметика fix-волны — склеенные строки в api-client.spec.ts:194. Из declined-to-judge финревью: duplicate-acceptance (ф.4, seq/cursor), ApiError status 0 на wire, каст `type as PublishRequest['type']` (план дословно, zod-гейт на месте). Подробности — леджер UI-среза.
- **Deferred-миноры батча 2 UI-среза (триаж финревью батча — все stay-deferred):** compile-time tripwire RoomErrorCode → STATUS_BY_WIRE_CODE (satisfies-подтаблица, схлопнет и cast `as WireCode`); mode-dependent API-404 wire-код (`NOT_FOUND` в static-режиме vs `REQUEST_INVALID` без — plan-mandated; клиентская сторона закрывается рекомендацией к Task 7); eslint-гард REQ-CORE-004 покрывает `**/*.ts`, не `.tsx` (widening глоба — при касании); `startsWith(prefix)` маршрутизировал бы гипотетический `/roomsX` в JSON 404 (SPA-маршруты не коллидируют). Из declined-to-judge финревью: HEAD на SPA-пути → JSON 404 (спека молчит, эффекта нет); `peerDependencyRules.allowedVersions` workspace-wide ключ — теоретическая широта при будущем втором потребителе react-19 types.
- **Deferred-миноры батча 1 UI-среза (триаж финального ревью батча — все stay-deferred, не load-bearing для батча 2):** io:'input' затрагивает event-payload артефакты (направление более корректное — гейт валидирует wire-форму; опциональный пин-тест); isUnrepresentablePipe строгость для transform-free pipes (fail-closed, задокументировано); механический bump version-dependent кейса contract-version.spec при каждом миноре; rosterResponseSchema/rosterRoleSchema без прямых кейсов (покрыты транзитивно); негативные manifestVersion на контрактном уровне SDK (нагружающая половина закрыта транспортным кейсом Task 5). Подробности — леджер UI-среза.
- **Deferred-миноры батча D (триаж финального целосрезового ревю ф.3 — все stay-deferred; подробности — batch-d-final-review.md):** комментарий "correct, second" в quiz-handlers.spec.ts:200; untyped `payload.awarded`; stale header identity-sweep.module.ts:9-10; revoke-after-COMPLETED не упражнён в e2e (тот же code path, что fulfill); REQ-SEC-009 displayName-ассерт только на draw.completed (reward.awarded покрыт int `rewards.int-spec.ts:102`); replay-чек новым гостем; waitEventWhere/emitAck без внутреннего таймаута; ANONYMIZATION_GUARDS в e2e boot-proven; теоретическая дыра barrel-transitive reach в boundary-правиле (grep чист, no-circular — backstop).
- **Deferred-миноры батча E (триаж таск- и финального ревью — все stay-deferred):** quiz-points.e2e — неиспользуемый orderBy createdAt (скетч-фиделити), waiters без внутреннего таймаута (прецедент), закрытие сокетов только на success-пути, DRAFT-строка теста 2 (чистится afterEach); целосрезовые кандидаты-заделы — статистическая равномерность randomInt (по духу §7.3), untyped `list.json().awards` в rewards.controller.int-spec:168. Все 28 deferred-строк батчей A–E протриажены финальным целосрезовым ревюм: must-fix ни одного (таблица — леджер/exit-аудит §5).
- **Deferred-миноры батча C (триаж финального ревью — все follow-up, не гейтят):** тест дубля T7 передаёт один object reference дважды (distinct literals — при касании); mid-commit failure с реальным handler не симулирован (rollback доказан спаем); неограниченная выборка кандидатов свипа / IN-список гарда (батчинг — задел); setInterval без overlap-защиты; TRUNCATE в guest-sweep.int-spec без `identity."Identity"` (count-ассерты опираются на порядок тестов); effect-only success-path (commits=[] + effects) покрыт только failure-веткой.
- **Deferred-миноры батча B (протриажены финальным целосрезовым ревюм ф.3 — все stay-deferred; подробности — леджер):** CHECK-presence тест ассертит conname без выражения (семантика запинана поведенческим P2010-тестом); тест award.points «события нет» без ассерта LogEvent.count (однострочник); createPrize check-then-insert TOCTOU (benign); race-loser в fulfill/revoke возвращает 200 с конкурирующим терминальным статусом вместо 409 (семантически defensible — decision-note); coverage `ROOM_NOT_ACTIVE` по HTTP только unit-уровнем; guest-scope гард транспорта RewardsController (defense-in-depth, эскалация недостижима — GUEST не может быть ORGANIZER; кандидат-задел); quiz-manifest.contract.spec компилирует схемы локальным Ajv БЕЗ addFormats — warning-шум `unknown format "uuid"` (косметика, парный фикс addFormats одним follow-up).
- ~~AJV warning `unknown format "uuid" ignored`~~ — **закрыто Task 1 батча A** (`420517a`): ajv-formats подключён на commit-гейте, enforcement `format: "uuid"` запинён тестом; invalid-uuid литералов в старых int/e2e-спеках не нашлось (все прогоны зелёные сразу — чистка не потребовалась).
- **Deferred-миноры батча A (протриажены финальным целосрезовым ревюм ф.3 — все stay-deferred):** дубликаты/пустой массив `capabilities` схемой не отсекаются (`.unique()` — при появлении второго capability); ts-jest слабее `tsc -p` (sdk test ≠ проверка типов — sdk build обязателен в гейте); wrapper `randomInt: (b) => randomInt(b)` — явная сигнатура контракта (фича, не шум).
- **Чистка fail-closed в `asLogEvent` (M-5)** + удаление мёртвой инжекции config (M-2) — пакет косметики realtime (часть про invalid-uuid литералы снята — их нет).
- **Косметика Quiz-среза (deferred minors, триаж «не гейтят»; полный список — леджер):** неограниченный рост `AppProjectionCache` (per-room записи без эвикции — фаза 4); clamp-ветка скоринга `Math.max(base−k·step,0)` без отдельного теста (провод защищён `points.min(0)`); quiz.e2e-spec.ts:379 мёртвый push closed-ack после проверочного цикла чит-теста; неотписанные live-listeners в чит-тесте; optionIndex-absence в `answer.accepted` проверен по одной ленте из четырёх; устаревший комментарий `realtime.e2e-spec.ts:89-90`; негативный кейс correctIndex неверного типа; литеральная роль `'participant'|'spectator'` в сигнатуре join (кандидат `JoinRequest['role']`); неограниченный рост spectators (кандидат формулировки при ревизии спеки).
- ~~Чистка invalid-uuid литералов~~ (снято Task 1 батча A — литералов нет). Остаток пакета косметики realtime — строкой выше.
- **Для следующего среза, трогающего configure/app-registry** (из финального ревью appSettings): guard `settings === undefined|null` → `AppSettingsInvalidError`; `ValidateFunction` из `ajv/dist/2020`; race-тест configure-vs-activate с quiz@2; контрактное допущение «settings — не-null JSON value».
- **Для web-client-среза:** CORS без `credentials: true` + SameSite=Strict — клиент с другого origin не сможет использовать refresh-куку (сейчас корректно для same-origin).
- Негативные strictObject-кейсы для ack-схем SDK (минор Task 2); экспорт realtime-фикстур из SDK index — при первом внешнем потребителе.
- Из membership/guest-join: гонка soft-delete/status-flip в `MembershipService.join` — принятый класс гонки (design fork (б)); JSDoc на `RoomService.create` про lowercase-in/Prisma-name-out.
- Deferred-миноры event-commit (ride): `eventValidatorFor` игнорирует schema-аргумент на cache-hit; некомпилируемая app-схема падает лениво (кандидат boot-time compile-check в app-registration срез); точки ручного конструирования `EventLogService` в спеках дрейфуют (кандидат — test-module builder).

## Два гейта над фазами

1. **Юрист** — до первого события с посторонними или призами. Список вопросов готов (`docs/legal/questions-for-lawyer.md`). Блокирует старт работы с реальными PII, не блокирует реализацию.
2. **Первое живое событие** — до тяжёлых вложений в фазу 3 (rewards/лотерея).

## Долгоживущие ограничения, введённые срезами

- **Замороженные миграции:** `20260718061612_room_lifecycle`, `20260722151900_identity_seam`, `20260722153952_room_organizer_fk`, `20260722180147_realtime_log_event`, `20260723090841_room_app_config`, `20260729164500_membership_guest_join`, `20260730101037_auth_sessions`, `20260819154552_membership_exclusion`, `20260902191322_oauth_identity_provider`, `20260910171833_rewards_schema`. Любое изменение — только новой миграцией. (Quiz-срез схему БД не трогал — состояние модуля живёт в логе событий.)
- **Postgres aborted-tx ловушка (F1 финального ревью батча B):** catch unique-violation ВНУТРИ транзакции отравляет её — все последующие операторы падают с 25P02, COMMIT aborted-транзакции молча откатывает. «Типизированный no-op» по частичному уникальному индексу — только через `INSERT ... ON CONFLICT (<колонки>) WHERE <предикат индекса> DO NOTHING` (`$executeRaw`, 0 строк → no-op до любых списаний); предикат конфликт-таргета обязан совпадать с предикатом частичного индекса 1:1, иначе inference не сработает. Прецеденты: `rewards.service.ts awardPrize` (ON CONFLICT), `identity.service.ts` (catch P2002 ВНЕ `$transaction` + свежее чтение). Учитывать при написании будущих планов — плановый verbatim-код с in-tx catch уже однажды прошёл планирование и был развёрнут финальным ревью.
- **Socket.io граница:** серверный `socket.io` импортируется только из `packages/core/src/realtime` (REQ-RT-006, правило dependency-cruiser с якорем на пакет `socket.io/`); `socket.io-client` — только `apps/server/test` (devDep). Правило круизит и dist (CI build→boundary-check) — в pathNot есть dist-исключение, осознанное.
- **Fan-out realtime:** publish в RealtimeBus — строго после коммита транзакции (`EventOutbox.run`); исключения слушателей изолированы в `fanOut` gateway (не отклоняют `run`); commit вне `outbox.run` бросает `EventOutboxMissingContextError` (fail-closed); вложенный `run` запрещён.
- **Subscribe:** join каналов — ДО чтения лога (duplicate-acceptance, дизайн §4); гейты (schema/guest-scope/мульти-подписка/membership) — до join; catch-путь чистит запись реестра.
- **Конвенция lockfile/CI:** один lockfile (REQ-DEV-002); CI порядок build → boundary-check.
- **Membership exclusion (срез 2026-09-02):** исключение — soft-delete membership (`deletedAt`) + строка `Exclusion` (unique(roomId,identityId), index(roomId,ip), FK RESTRICT) + отзыв guest-сессий — одной транзакцией; повторный exclude — typed no-op (идемпотентность). `findActiveMembership` гасит soft-deleted; deletedAt-гейт стоит и в `commitAppEvent`, и в `participantCount` (дочистка read-paths, `06c1019`). Rejoin-блок — по `joinIp` через `Exclusion` в `join` (REQ-ID-006 ч.3). Организатор неисключаем, его `joinIp = null` (nullable — санкционированное отклонение от дизайна §2).
- **Hook `onAccessRevoked` (MembershipService → RealtimeGateway):** тип `void | Promise<void>`, fan-out sequential-await, исключения слушателей изолированы (sync-throw и async-reject логируются, не роняют exclude). `RealtimeGateway.revokeRoomAccess` — sync `void`: разрыв подписки + leave каналов + отзыв сокета.
- **Barrel core:** `ActorNotMemberError` экспортируется realtime-версией (явный re-export в `index.ts`, TS2308 от одноимённых классов); membership-версия потребляется прямым путём модуля. Barrel-потребителей класса нет.
- **Prod-баррел core тянет testcontainers в require-time** (design §9 — осознанное расширение ради e2e). Безопасно, пока Dockerfile тащит полные `node_modules` в runtime-стейдж; упадёт при pruning devDependencies. Follow-up-кандидат: вынести testing в отдельный entry point (`@mymozhem/core/testing`); не давать workaround с dist-subpath-импортами (create-room.mjs) стать постоянным.
- **`NODE_OPTIONS=--experimental-vm-modules` зашит в test-скрипт apps/server** — `@fastify/cookie@11` динамически импортирует ESM-only `cookie@2`, что ломает jest 29 CJS. Изолирован в test-скрипте. Триггеры пересмотра: jest 30 или `@fastify/cookie` на `require(ESM)` (Node 24).
- **Refresh-кука `Secure` при `NODE_ENV=production`** — compose-смоук по plain HTTP не сможет round-trip куки cookie-jar клиентом; ассертить `Set-Cookie`-заголовок.
- **Конвенция порядка блокировок:** advisory lock комнаты — всегда leaf-most; транзакция, захватившая его, не должна после этого писать в `room."Room"`.
- **Prisma 7.8 adapter-pg ловушка:** `$queryRaw` не десериализует `void`-возвращающие выражения (`pg_advisory_xact_lock`) — использовать `$executeRaw`. Учитывать при написании будущих планов.
- **Prisma 7.8 adapter-pg: форма ошибок raw-запросов.** Падающий `$queryRaw` оборачивается в `PrismaClientKnownRequestError` с кодом `P2010`; SQLSTATE внутри message и `meta.driverAdapterError.cause.originalCode`. Матчить `code === 'P2010'` + подстроки (прецедент `isRoomCodeCollision`). Также: `$queryRaw` возвращает сырое DB-значение enum (`'public'`), а не Prisma-имя (`'PUBLIC'`) — для staged-событий нормализует `asLogEvent` (realtime-срез); при `RETURNING *` из raw INSERT — re-read через клиент (прецедент `insertRoom`) или явная нормализация.
- **Инвариант «change both or neither» (ТРИ места):** предикат `kind = 'REGISTERED' AND deletedAt IS NULL` живёт в частичном индексе `"Identity_registered_email_key"`, guarded INSERT в `RoomService.create` и пречеке конфликта в `IdentityService.findOrCreateByProvider`. Менять только вместе. (Комментарий в `room.service.ts:82-83` всё ещё говорит «два места» — известный дрейф, поправить при следующем касании.)
- **Хост-порты 5432 и 55432:** контейнеры `lt-pg`/`lt-pg-sdd` (не проектные) были удалены 2026-09-03 рестартом OrbStack и не восстановлены — перед публикацией authoring-контейнера проверять `docker ps`.
- **Testcontainers v12 + OrbStack — две ловушки restart'а контейнера:** (1) у Started-контейнера нет `start()`, stop/start идут через `getContainerRuntimeClient()` (`client.container.stop/start(getById(id))`); (2) **OrbStack переназначает случайный host-порт при restart** — старый пул мёртв навсегда, клиент пересоздаётся на фактический порт из `inspect`. Оба приёма зашиты в харнесс (`stopContainer`/`startContainer`, последний возвращает новый `PrismaService`).
- **`prisma migrate dev` не всегда регенерирует клиент; явный `pnpm exec prisma generate` требует DATABASE_URL** и cwd = корень репозитория. Prisma 7 CLI работает только из корня репо (`prisma.config.ts` там): форма `pnpm --filter @mymozhem/core exec prisma …` падает — использовать `pnpm exec prisma …` из корня.
- **`packages/core/src/testing/postgres.testcontainer.ts` — переиспользуемый паттерн ядра.** Мутирует глобальный `process.env.DATABASE_URL` без восстановления (безопасно только при `maxWorkers: 1`); требует cwd = корень репозитория. Несёт `stopContainer`/`startContainer` для тестов недоступности БД (REQ-DEV-008, `db-down.int-spec.ts`).
- **`ReplyLike` — адаптер fastify 5:** сигнатуры сверять с fastify 5 (`redirect(url, code?)`, code дефолт 302). OAuth-куки (`mm_oauth_*`) — `SameSite=Lax` осознанно (возврат с Google — top-level GET), path `/auth`, TTL `OAUTH_STATE_TTL`; refresh-кука остаётся `Strict`. Гостевой cap refresh — ТОЛЬКО в точках выдачи (`TokenService.sessionExpiry` ⇄ `setRefreshCookie` — «одна норма в двух местах», менять вместе); конфиг-инварианта `REFRESH ≤ GUEST_TTL` больше нет.
- **Прогон интеграционной ланы поднимает контейнеры Postgres** (~8 с локально на файл). Docker Desktop/OrbStack должен быть запущен.
- **Jest CLI:** форма `pnpm --filter @mymozhem/core test:int -- -t "..."` миспарсится — рабочая форма без `--`. Фильтр всегда проверять на >0 матчей. В worktree с говорящим именем testPathPattern матчит путь целиком (см. опыт Quiz-среза).
- **apps/server e2e резолвит `@mymozhem/core` из dist** — перед `pnpm --filter @mymozhem/server test` обязателен `pnpm build`; core резолвит `@mymozhem/sdk` из dist — после правок SDK `pnpm --filter @mymozhem/sdk build`; то же для `@mymozhem/app-quiz` (server e2e тянет его dist).
- **Граница app-модулей (Quiz-срез):** `packages/app-*` НЕ импортирует `packages/core` (boundary `app-only-through-sdk`, правило покрывает `packages/app-` generically); `@mymozhem/sdk` — лист. Связывание — только `apps/server` (composition root).
- **Контракт SDK 1.6.0 (батч A ф.3, `420517a..5729fb2`):** ajv-formats подключён в `AppRegistryService` — `format: "uuid"` принуждается на commit-гейте (невалидный uuid в payload = `EVENT_PAYLOAD_INVALID` до коммита; zod v4 `z.uuid()` требует RFC 9562 version nibble). `handlePublish` может вернуть `{commits, effects}`; старая форма «голый массив» читается `normalizePublishResult` (обратная совместимость, квиз v1 не тронут). **Эффекты fail-closed до Task 7:** любой эффект → `CAPABILITY_UNAVAILABLE` до коммита; `drawPool` в ctx всегда пуст до Task 7. `AppHostContext` += `randomInt` (node:crypto, единственный санкционированный источник случайности, REQ-RWD-011) и `drawPool`. `REWARDS_EVENTS` (reward.awarded/fulfilled/revoked, public, v1, payload — только id) — коммитит ядро (Task 5), не app-модули. `ROOM_NOT_ACTIVE` выведен в `CONTRACT_ERROR_CODES` (интерпретация №7).

## Отложенные follow-up (не гейтят; полный список с обоснованиями — в леджерах)

- **OAuth-срез (триаж финального ревью — все follow-up):** unit на `setRefreshCookie` (maxAge-тернарник покрыт только зеркально); `createRoomResponseSchema.parse` на границе POST /rooms (сейчас echo-инвариант на контракте сервиса); deletedAt-гард в P2002 winner-ветке `findOrCreateByProvider`; cross-account email-гонка → raw P2002 → 400 вместо OAUTH_EMAIL_CONFLICT (различать индекс по `err.meta.target` — дизайн-решение); очистка oauth-кук при rate-limit/zod-отказах callback (сейчас живут до TTL ≤ 600 с — не ослабление, сознательно); поправить комментарий «два места» в `room.service.ts`; опечатка REQ-ID-015→REQ-ID-005 в комментарии `ROOM_ORGANIZER_NOT_REGISTERED` (sdk error-codes); непокрытые мелочи тестов: Max-Age/Secure oauth-кук, start-лимитер, точный статус /rooms/join в REQ-SEC-006 батарее; `oauthCallbackQuerySchema` strict — лишний query-параметр (utm_*) даст 400, помнить при web-client-срезе.

- **Вынести testing-экспорты из prod-баррела core** в отдельный entry point — при этом починить `create-room.mjs` обратно на баррел (его dist-subpath-импорты молча сломаются при появлении `exports` в core). После realtime-среза скрипт дополнительно конструирует RealtimeBus/EventOutbox.
- **`create-room.mjs`: findFirst по точному `email`, а индекс — по `lower(email)`** — при кейс-варианте будет сырая unique-violation вместо reuse.
- **`updateManyAndReturn` доступен на Prisma 7.8.0** — схлопнет 3 запроса в 2 в мутациях `RoomService` (transition/softDelete/configure). **Осторожно:** transition теперь работает внутри `outbox.run` с staging — рефакторинг не должен разорвать атомарность «UPDATE + лог + staged-доставка».
- **Нет гейта на дрейф миграций** (`prisma migrate diff --from-migrations` непригоден без `datasource.shadowDatabaseUrl` в `prisma.config.ts`).
- **Общая рекурсивная `jsonValueSchema`** для payload в `log-event`/`projected-event` — `z.record(z.string(), z.unknown())` не принуждает структурно REQ-CTR-002.
- Косметика: breadcrumb в `schema.prisma` о рукописном CHECK; ассерт ортогональности soft-delete только для CANCELLED.

## Осталось недоделанным

- **Батч 5 UI-среза (Tasks 13–15) новой сессией (subagent-driven, worktree `.worktrees/ui-slice`):** см. «Следующее действие» — ритм «3 задачи = 1 сессия» (решение владельца 2026-09-23), всего 6 батчей, завершены 4. **Manual smoke OAuth с реальными Google-кредами** перед первым живым событием — действие владельца (чек-лист готовит Task 17 среза). **Push main на origin** (`origin/main` = `30545fb`) и **мердж ветки `ui-slice` в main** (батчи 1–4) — решения владельца. **Решение о React 19/react-router 7:** батч 2 запинал react 18/router 6 по утверждённому плану; если владелец хочет текущие мажоры — отдельное решение с правкой дизайна §2. **Снятие worktree `.worktrees/rewards-lottery`** — отложенное решение владельца. **Кандидаты владельцу из батча 4:** eslint-plugin-react-hooks; серверный фикс spectator refresh-куки (см. Follow-up пакеты).
- **Вопросы юристу не заданы** — гейт 1 открыт, действие вне агента.

## Session 2026-09-24 (двадцатая: батч 4 UI-среза — исполнение subagent-driven)

*(Блок девятнадцатой сессии (батч 3 UI-среза) вырезан — работа в коммитах; восстановим через `git show 02aac53:HANDOFF.md`. Более ранние: сессия 18 — `git show 7c92496:HANDOFF.md`, сессия 17 — `git show cd95fed:HANDOFF.md`, сессия 16 — `git show 6bb4190:HANDOFF.md`.)*

### Что сделано

- **Исполнен батч 4 UI-среза (Tasks 10–12) — subagent-driven** (fresh implementer + task-ревью на задачу + финревью батча на fable; pre-flight scan переиспользован из леджера — строки T10/T11/T12 чистые). Работа — в worktree `.worktrees/ui-slice` (ветка `ui-slice`). Инцидент: ревьюер Task 11 прерван сбоем/перезагрузкой сессии, возобновлён SendMessage'ом — read-only, потерь нет.
  - **Task 10 (`2a96c23`):** SessionStore + useRoomBinding — ревью чистое (spec ✅, Approved), 3 minor deferred. Рекомендации финревью батча 3 (a: join один раз на effect; b: стартовый отказ join → 'disconnected'; c: исправленный факт о gateway) унесены в диспатч и запинаны.
  - **Task 11 (`924b319`):** /play + shared-компоненты — ревью чистое, 5 minor deferred. Rulings в диспатче: anonClient (null-TokenProvider), ConnectionBanner не при roomId===null, roster через SessionStore-backed клиент.
  - **Task 12 (`fa030a9`):** /screen — ревью чистое, 5 minor deferred. Ruling: screen НЕ вызывает saveGuest (spectator не затирает guest-сессию участника).
- **Финревью батча (fable): With fixes.** Critical: reveal-вердикт play-page — мёртвый код (quiz.answers всегда пуст на клиенте — answer.submitted module-private; класс «виден только кросс-пакетной трассировкой visibility»). Important: reconnect после протухания access-токена никогда не восстанавливался (refresh триггерился только REST 401 — доминирующая причина обрыва на живом событии). Все 10 wire-checks прошли (пути/схемы/коды сверены с transport/sdk/app-quiz).
- **Fix-волна (`2a33aae`):** клиентский фолд публичного `quiz.answer.accepted` (`projectAnsweredActors`, keyed по questionIndex) → вердикт из reveal.awarded; refresh-хук на connect_error (один на outage, fail → 'disconnected'). **Re-review поймал новый Critical от самой волны:** useMemo answeredActors после early return if (!roomId) → rules-of-hooks крах на каждом входе в комнату (верифицировано контроллером). **Точечный фикс (`d71022c`):** хук поднят выше early return (placement-only) → **micro re-review чистое** (все 15 хуков безусловны, логика byte-identical).
- **БАТЧ 4 ЗАВЕРШЁН** (`e5caa45..d71022c`, 5 коммитов). web 49/49, lint/typecheck/build/boundary зелёные. Мердж в main — решение владельца.

### Коммиты этой сессии

- `2a96c23` feat(web): SessionStore + useRoomBinding (snapshot/resync фолд в React) *(ui-slice)*
- `924b319` feat(web): страница участника /play + shared-компоненты (standings/winners/banner) *(ui-slice)*
- `fa030a9` feat(web): проекторный экран /screen (spectator-join, read-only) *(ui-slice)*
- `2a33aae` fix(web): reveal-вердикт из публичного answer.accepted + refresh токена при reconnect (UI-срез, финревью батча 4) *(ui-slice)*
- `d71022c` fix(web): порядок хуков в play-page — useMemo до early return (UI-срез, re-review батча 4) *(ui-slice)*
- (далее) docs(handoff): update for session 2026-09-24 — батч 4 UI-среза исполнен *(main)*

### Локальное состояние (не в git)

- Worktree `.worktrees/ui-slice` (ветка `ui-slice` @ `d71022c`) — активный срез; `.worktrees/rewards-lottery` не тронут (снятие отложено).
- Леджер SDD `.superpowers/sdd/2026-09-23-ui-slice-implementation-plan/progress.md` в основном чекауте: батчи 1–4 со всеми ревью/триаж/rulings (вкл. уроки: клиент не видит module-private события; нет eslint react-hooks гейта), рекомендации батчам 5/6; briefs и отчёты (`task-N-brief.md`/`task-N-report.md`, `batch-4-final-fix-report.md`) — там же; diff-пакеты ревью — в `.superpowers/` worktree. **Не в git; `git clean -fdx` уничтожит.**
- Side-effects на внешние системы: нет (push/мердж — решения владельца).

### Осталось недоделанным

- См. одноимённый раздел выше (батчи 5–6 UI-среза; мердж ui-slice; push; решение о React 19; кандидаты владельцу из батча 4 — eslint react-hooks, spectator refresh-кука; manual smoke OAuth; юрист; снятие worktree rewards-lottery).
